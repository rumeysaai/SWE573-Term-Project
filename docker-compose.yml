
services:
  # Veritabanı Servisi (PostgreSQL)
  db:
    image: postgres:latest
    volumes:
      - postgres_data:/var/lib/postgresql
    # 2. .env dosyasındaki değişkenleri buraya bağlayın
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5  
    networks:
      - internal_bridge

  # Backend Servisi (Django)
  backend:
    build: ./backend
    networks:
      - internal_bridge # EKLE: Frontend'in backend'i bu isimle görmesi için
      # - dokploy-network # EKLE: Traefik ile iletişim için (commented out for local testing)
    env_file: .env
    volumes:
      - ./backend:/app # Kodu konteynere bağla (geliştirme için)
    environment:
      # 3. Django'nun doğru host'a (db) bağlanmasını sağlayın
      DB_HOST: db
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      SECRET_KEY: ${SECRET_KEY}
    depends_on:
      db:
        condition: service_healthy
    command: >
      sh -c "python manage.py migrate &&
             gunicorn the_hive.wsgi:application --bind 0.0.0.0:8000"

  # Frontend Servisi (React + Nginx)
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - internal_bridge # EKLE: Backend'i görebilmesi için KRİTİK!
      # - dokploy-network # EKLE: Traefik ile iletişim için (commented out for local testing) 

volumes:
  postgres_data: {}

networks:
  # dokploy-network:  # Dokploy'un Traefik Ağı (Dışarıdan erişim ve SSL) - commented out for local testing
  #   external: true
  internal_bridge:  # Servislerin birbirini görmesi için özel bir ağ
    driver: bridge